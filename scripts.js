// Function to view specific record
function viewRecord(recordName) {
    alert("You are about to view: " + recordName);
    // In a real scenario, this would navigate to the detailed record page
    // or display a modal with the record details.
    // Example:
    // window.location.href = 'record-details.html?record=' + encodeURIComponent(recordName);
}
